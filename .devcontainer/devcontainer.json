{
  "name": "task-planner-comfort",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:20",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  },
  "forwardPorts": [3000],
  "portsAttributes": {
    "3000": {
      "label": "Next.js Dev Server",
      "onAutoForward": "openBrowser"
    }
  },
  "postCreateCommand": "npm ci",
  "postStartCommand": "if [ -f /tmp/task-planner-dev.pid ]; then PID=$(cat /tmp/task-planner-dev.pid); if [ -d /proc/$PID ] && [ \"$(readlink /proc/$PID/cwd 2>/dev/null)\" = \"$PWD\" ] && tr '\\0' ' ' < /proc/$PID/cmdline | grep -q 'next dev -H 0.0.0.0 -p 3000'; then echo dev-server-already-running; else rm -f /tmp/task-planner-dev.pid; fi; fi; if [ ! -f /tmp/task-planner-dev.pid ]; then nohup npm run dev >/tmp/task-planner-dev.log 2>&1 & echo $! >/tmp/task-planner-dev.pid; fi"
}
